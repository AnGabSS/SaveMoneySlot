server:
  port: 8080
spring:
  application:
    name: SaveMoney$lot
  profiles:
      active: prod

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs