spring:
  application:
    name:
      SaveMoney$lot
  config:
    import: optional:configserver:http://config-server:8888/
  cloud:
    config:
      label: main
  flyway:
    baseline-on-migrate: true